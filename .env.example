# Copy to .env.local and fill in. Never commit the filled-in file.
# Production points at the API subdomain so the admin auth cookie is first-party (P6-D):
#   NEXT_PUBLIC_API_URL=https://api.snaapconnections.co.ke/api
NEXT_PUBLIC_API_URL=
